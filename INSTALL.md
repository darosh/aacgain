# Install

aacgain is a command-line tool for applying Replay Gain normalization to AAC and MP3 files.

## macOS (Homebrew)

```sh
brew install darosh/tap/aacgain
```

> On first run macOS may show a privacy prompt — allow it in System Settings → Privacy & Security.
